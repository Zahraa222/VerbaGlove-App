// Learn more: https://docs.expo.dev/guides/using-expo-client/#bare-workflow

const { getDefaultConfig } = require('@expo/metro-config');

const config = getDefaultConfig(__dirname);

module.exports = config;
